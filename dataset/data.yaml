# Class definition for GrowingSoy. Splits are not fixed. Generate them with
# experiments/make_folds.py, which groups images by recording pass, or with
# experiments/make_original_split.py, which assigns them at random. Each writes
# its own data.yaml for every fold.

path: .
images: images
labels: labels
metadata: metadata.csv

nc: 3
names: ['caruru_weed', 'grassy_weed', 'soy_plant']
